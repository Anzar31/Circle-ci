version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run: "python main.py"
      - run: "python test.py"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello